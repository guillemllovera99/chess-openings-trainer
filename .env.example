# Path to the Stockfish engine (use Homebrew-installed version)
ENGINE_PATH=$(which stockfish)

# Engine analysis depth (higher = slower but stronger)
ENGINE_DEPTH=16

# Number of candidate moves to show (MultiPV)
ENGINE_MULTIPV=3

# Neural network checkpoint + optimizer state
MODEL_PATH=./models/nn/checkpoints/latest.ckpt
OPTIMIZER_PATH=./models/nn/optimizer_state/latest.opt
